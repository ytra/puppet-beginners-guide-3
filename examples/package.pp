package { 'cowsay':
  ensure => installed,
}

package { 'tmux':
  ensure => installed,
}

package { 'sysdig':
  ensure => installed,
}

package { 'atop':
  ensure => installed,
}

package { 'htop':
  ensure => installed,
}

package { 'dstat':
  ensure => installed,
}
